{
  "name": "@template/wasm",
  "version": "0.0.1",
  "description": "",
  "main": "dist/wasm.d.ts",
  "types": "dist/wasm.d.ts",
  "module": "dist/wasm",
  "files": [
    "dist"
  ],
  "keywords": [],
  "scripts": {
    "prepare": "sh ./scripts/env.sh",
    "dev": "cargo watch -s \"wasm-pack build --target web --out-dir ./dist --no-package\"",
    "build": "rimraf ./dist && wasm-pack build --target web --out-dir ./dist --release --no-package"
  },
  "license": "Proprietary"
}
